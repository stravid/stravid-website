<!DOCTYPE HTML>
<html lang="<%= @item[:language] %>">
  <head>
    <meta charset="utf-8">
    <meta name="description" content="<%= @item[:description] %>">

    <title><%= @item[:title] %></title>
  </head>
  <body>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
